# forestvisionlab.github.io

Forest Vision Lab – research in under-canopy imaging, forest sensing, and hidden terrain reconstruction.

## Updating the project page

1. Edit `site.json` to update the title, abstract, authors, links, and citation.
2. Upload assets to the `assets/` folders (see `assets/README.md` for details).
3. Open `index.html` locally or via GitHub Pages to verify the updates.
